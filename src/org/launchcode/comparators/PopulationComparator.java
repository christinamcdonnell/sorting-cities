package org.launchcode.comparators;

import org.launchcode.City;

import java.util.Comparator;

/**
 * Created by Christy on 6/15/2017.
 */
public class PopulationComparator implements Comparator<City> {

    @Override
    public int compare(City o1, City o2) {

        //return o1.getPopulation().compare(o2.getPopulation());

        return Integer.compare(01.getPopulation(), 02.getPopulation());
        if(01.getPopulation() < 02.getPopulation() )
    }
}
